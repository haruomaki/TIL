# 幾何分布

## 確率質量関数

```math
\begin{equation}
P(X=k) = (1-p)^kp
\end{equation}
```

## 確率母関数・特性関数・キュムラント母関数

```math
\begin{aligned}
G(s) = 𝔼[s^X] &= \sum_{k=0}^∞ s^k \cdot (1-p)^kp \\
              &= p \sum_{k=0}^∞ [s \cdot (1-p)]^k \\
              &= \frac{p}{1 - s(1-p)} \\
\end{aligned}
```

```math
\begin{equation}
φ(t) = G(e^{it}) = \frac{p}{1 - e^{it}(1-p)}
\end{equation}
```

```math
\begin{equation}
ψ(t) = \log φ(t) = \log p - \log \left( 1 - e^{it}(1-p) \right)
\end{equation}
```

## 期待値

```math
ψ'(t) = \frac{ie^{it}(1-p)}{1 - e^{it}(1-p)}
      = \frac{i}{{1 - e^{it}(1-p)}} - i \\
𝔼[X] = \frac{ψ'(0)}{i} = \frac{1-p}{p} \\
```

## 分散

```math
ψ''(t) = \frac{- e^{it}(1-p)}{\left( 1 - e^{it}(1-p) \right)^2} \\
Var[X] = \frac{ψ''(0)}{-1} = \frac{1-p}{p^2} \\
```
